# cranberry-rna

`cranberry-rna` is the installable Python package for CRANBERRY coarse-grained RNA simulations with OpenMM.

The Python import package is `cranberry`:

```python
import cranberry
```

This package is currently in alpha development while code is migrated from the legacy `OpenMM-CGRNA` project.
