# Benchmark Results

Raw benchmark JSON snapshots generated by `benchmarks/benchmark_md.py` live here.

File names should encode the run label, platform, and whether MPS was enabled so the snapshot can be compared across machines and clusters.
