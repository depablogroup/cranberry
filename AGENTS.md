# Cranberry Package Instructions

This is the new `cranberry-rna` distribution. The Python import package is `cranberry`.

Before changing package architecture, force-field behavior, CLI contracts, output formats, or tests, read `docs/dev/cranberry-v1-plan.md`.

Keep this file concise. Put long design notes, migration notes, and release decisions in `docs/dev/`.
